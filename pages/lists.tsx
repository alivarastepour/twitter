import HomePageWrapper from "../Components/HomePageWrapper";
import Spinner from "../Components/Spinner";

const ListsPage = () => {
    return <>
        <HomePageWrapper props={{}} Component={Spinner} title='list'/>
    </>
}

export default ListsPage;