import styled from "styled-components";

export const Wrapper = styled.div`

  .visibility {
    margin-left: 80px;
  }

  .visibilityStatus {
    font-size: 1.0rem;
    color: var(--twColor);
    border-radius: 5px;
    display: flex;

    .visibilityIcon {
      margin-right: 10px;
    }
  }
`;